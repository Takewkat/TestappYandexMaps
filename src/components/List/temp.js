//реализация Листа без drag & drop
/*

function List ({ points, deletePoint }) {
  
    return (
      <ul>
        { points.map(p =>
          <li key={ p.id } className="list-item">
            <span>{ p.label }</span>
            <button onClick={ () => deletePoint(p.id) }>X</button>
          </li>
        )}
      </ul>
    );
  };
  
export default List;
*/